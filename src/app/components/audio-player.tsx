"use client";

export const AudioPlayer = ({ src }: { src: string }) => {
  return (
    <div className="flex h-16 w-full items-center gap-4 rounded-full bg-white px-4 text-black"></div>
  );
};

export default AudioPlayer;
